# =============================================================================
# NFL Predictor — local configuration
# Copy this file to .env and fill in your values. .env is gitignored.
# =============================================================================

# --- The Odds API (optional) -------------------------------------------------
# Free key at https://the-odds-api.com/ — betting lines factor is skipped if absent.
ODDS_API_KEY=

# --- Factor weights ----------------------------------------------------------
# These are your "secret sauce". The engine normalises whatever values you set
# so they don't need to sum to 1.0 — just express relative importance.
# Remove the betting lines weight (or set to 0) if you don't have an API key.
WEIGHT_RECENT_FORM=
WEIGHT_HOME_AWAY=
WEIGHT_HEAD_TO_HEAD=
WEIGHT_BETTING_LINES=

# --- Factor tuning -----------------------------------------------------------
# How many past games to include in recent-form calculation.
RECENT_FORM_GAMES=
# Geometric decay applied per game further back in time (0.0–1.0).
# Higher = more weight on recent games; lower = more even distribution.
RECENT_FORM_DECAY=
# Maximum number of head-to-head meetings to look back across seasons.
H2H_GAMES=
